# v0.5.4 — finalny wąski re-review B4/F1

Data: 2026-09-09  
Kandydat: `feature/v0.5.4-work-time-types-hardening`  
Commit reworku: `7f3657322086ae234e04b88e57d836bf8bd73091`

## Werdykt

**B4/F1: RESOLVED**

**READY TO MERGE INTO DEVELOPMENT: YES**

Zakres tego raportu jest ograniczony do wcześniejszego blockera B4/F1. Nie wykonano pełnego review wersji ani żadnych operacji merge, commit, push, tag, release lub deploymentu.

## Niezależnie potwierdzone elementy

- Upgrade v0.5.3 → v0.5.4 tworzy stan pre-v0.5.4 przez `prisma migrate deploy` na kopii rzeczywistych pierwszych dziewięciu migracji. Test potwierdza poprawną historię `_prisma_migrations` (9 zakończonych migracji przed upgrade'em i 10 po nim, z kandydacką migracją bez `rolled_back_at`).
- Upgrade kandydata wykonuje `npx prisma migrate deploy`, następnie skompilowany artefakt `node dist/prisma/seed.js`, zgodnie z kolejnością produkcyjnego `docker-entrypoint.sh`. Test nie używa raw `psql` do wykonania migracji v0.5.4 ani `ts-node` jako zastępstwa production seeda.
- Scenariusz zawiera wszystkie customowe kolizje: `WKU`, `OP`, `NN`, `NU`, `NUN`, `NUP`, `UB`, `UO`, `UPP`; dla każdej istnieje porównanie BEFORE/AFTER pól `name`, `isSystem`, `requiresOrder`, `isAbsence`.
- Customowy `WKU` zachowuje `name` oraz `isSystem=false`, a zmienia wyłącznie zatwierdzone flagi: `requiresOrder=false`, `isAbsence=true`. Pozostałe osiem kodów pozostaje niezmienione, również z niezmienionym `updatedAt`.
- Historyczne `work_time_reports` dla wszystkich kolizji oraz rekordów kontrolnych zachowują liczbę rekordów, kod typu i godziny.
- Test API potwierdza, że customowy rekord `isSystem=false` może być edytowany; usunięcie customowego rekordu z raportami jest blokowane zależnością historyczną, a customowy rekord bez raportów można usunąć. Ochrona systemowego `G` pozostaje aktywna.
- Ten sam ciąg `prisma migrate deploy` + `node dist/prisma/seed.js` jest ponawiany po upgrade'ie i pozostaje idempotentny. Fresh install korzysta z identycznego ciągu i również jest powtarzalny.

## Wykonane walidacje

| Polecenie | Wynik |
| --- | --- |
| `cd backend && npm exec vitest run tests/executable-database-migration.test.ts` | PASS — 5/5 |
| `cd backend && npm test` | PASS — 15 plików, 192/192 |
| `cd backend && npm run build` | PASS |
| `cd backend && DATABASE_URL='postgresql://localhost:5432/workshop_time_tracking' npx prisma validate` | PASS |
| `cd backend && DATABASE_URL='postgresql://localhost:5432/workshop_time_tracking' npx prisma generate` | PASS |
| `git diff --check` | PASS |

Dedykowany test uruchomiono na efemerycznym kontenerze PostgreSQL; nie użyto ani nie modyfikowano bazy produkcyjnej.
