# Local backend configuration (not used by Docker Compose).
# Copy to backend/.env and replace CHANGE_ME values before starting the API.
DATABASE_URL="postgresql://time_user:CHANGE_ME@localhost:5432/time_reporting?schema=public"
JWT_SECRET=""
PORT=5000
LOG_LEVEL=debug
