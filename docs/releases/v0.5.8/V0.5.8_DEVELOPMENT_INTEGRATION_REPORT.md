# v0.5.8 Development Integration Report

- Timestamp: 2026-09-17T08:30:11Z
- Feature branch: `feature/v0.5.8-calendar-aware-default-work-type`
- Feature HEAD actually merged: `324731a1f3335092a86f64cb137ed10dc150e999`
- Previous development SHA: `103461bbadb367733be3c15bb5cbe1a66bc38066`
- Development merge commit: `57b1c0996f0bbca4aa080b284a7f3307d46e4bf2`
- Version bump commit: `64c3719f2d5bacd1fd91f7ae69b240dd05aa499e`
- Final development SHA before this report: `64c3719f2d5bacd1fd91f7ae69b240dd05aa499e`

## Version bump

The development application version was changed to `0.5.8` in:

- `backend/package.json`
- `backend/package-lock.json`
- `frontend/package.json`
- `frontend/package-lock.json`
- `docker-compose.yml`
- `docker-compose.homelab-dev.yml`
- `README.md`
- `CHANGELOG.md`
- `PROJECT_STATUS.md`

No production release heading, tag, GitHub Release, or main merge was created.

## Review gate status

- FRONT-001: RESOLVED by the independently reviewed feature correction.
- DOC-001: RESOLVED by the independently reviewed user-guide correction.
- OBS-001: OPEN — NON-BLOCKING; fallback calendar decisions remain component-lifetime cached after a transient endpoint failure, as explicitly accepted by the independent re-review.
- Manual acceptance: PENDING.

## Validation

Targeted validation:

- `cd frontend && npm test -- --run src/test/ReportingPanel.test.tsx`: **38/38 passed**.
- `cd backend && npm test -- --run tests/company-calendar.test.ts`: **10/10 passed**.
- `cd backend && npm test -- --run tests/analytics.test.ts`: **62/62 passed**.

Full validation:

- `cd backend && npm test`: **198/198 passed** across 15 files.
- `cd backend && npm run build`: **PASS**.
- `cd frontend && npm test`: **115/115 passed** across 10 files.
- `cd frontend && npm run lint`: **PASS**.
- `cd frontend && npm run build`: **PASS**.
- `DATABASE_URL='postgresql://audit:unused@localhost:5432/audit' backend/node_modules/.bin/prisma validate --schema backend/prisma/schema.prisma`: **PASS**.
- `docker compose -f docker-compose.homelab-dev.yml config` with safe runtime variables: **PASS**.
- `git diff --check`: **PASS**.

The frontend build emitted the existing Vite/esbuild deprecation warning; it did not fail validation. Docker image builds completed successfully.

## Development runtime

Only the DEVELOPMENT homelab Compose stack was rebuilt, using `docker-compose.homelab-dev.yml` and the existing external volume `workshop-time-tracking-dev_pgdata`. No database reset, migration reset, seed, truncate, or volume removal was performed.

- `worktime-dev-db`: running and healthy.
- `worktime-dev-api`: running and healthy.
- `worktime-dev-web`: running.
- Backend health: `GET http://127.0.0.1:5085/api/health` returned status `ok` with database `ok`.
- UI: `GET http://127.0.0.1:8085/` returned HTTP 200.
- Visible application version: `0.5.8`, verified by `GET http://127.0.0.1:5085/api/version`.

## Compatibility and change assessment

- Database migration required: **NO**; v0.5.8 changes do not alter the Prisma schema.
- API breaking change: **NO**; the calendar endpoint and existing report/analytics contracts remain compatible.

The v0.5.6 historical reconciliation behavior and v0.5.7 cumulative progress behavior are covered by the passing integrated backend tests, including the targeted analytics suite.

## Repository safety

- `main` untouched.
- `demo` untouched.
- Client production untouched.
- No tag or GitHub Release created.
- No deployment outside the DEVELOPMENT runtime performed.

Final development SHA after this documentation report commit is to be recorded by the commit that adds this file.

